import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { RouterLink } from '@angular/router';
import { ErrorStateComponent, SkeletonComponent } from '../../design-system/feedback/feedback';
import { DashboardStore } from './dashboard.store';
import { QueueItem, mapQueueItem } from './dashboard.models';

@Component({
  selector: 'app-home-actions',
  imports: [RouterLink, ErrorStateComponent, SkeletonComponent],
  template: `<div class="action-column">
    <section class="surface-panel attention-section" aria-labelledby="attention-title">
      <div class="section-heading"><div><span class="section-kicker attention-kicker">AGORA</span><h2 id="attention-title">Atenção operacional</h2></div>@if(store.attention().status==='ready'&&store.attention().value;as attention){<span class="attention-count" [class.empty]="!attention.preview.length">{{attention.preview.length}}</span>}</div>
      @if(store.attention().status==='ready'&&store.attention().value;as attention){
        @if(attention.preview.length){<div class="queue" role="list">@for(item of attention.preview;track item.stableId){@let entry=queue(item);<div class="queue-entry" role="listitem" [class.critical]="entry.status==='OVERDUE'"><button type="button" class="queue-button" [attr.aria-expanded]="openItem()===entry.id" (click)="toggleItem(entry.id)"><span class="domain-dot" [attr.data-domain]="domain(entry.kind)" aria-hidden="true"></span><span class="queue-copy"><strong>{{entry.title}}</strong><small>{{entry.context || kindLabel(entry.kind)}} · {{sourceLabel(entry.source)}}</small></span><time [attr.datetime]="entry.date">{{shortDate(entry.date)}}</time><span class="queue-chevron" aria-hidden="true">›</span></button>@if(openItem()===entry.id){<div class="queue-detail"><span>{{kindLabel(entry.kind)}}</span><span>Data operacional: {{formatDate(entry.date)}}</span>@if(entry.status){<span>Situação: {{statusLabel(entry.status)}}</span>}</div>}</div>}</div>}
        @else{<div class="attention-clear"><span aria-hidden="true">✓</span><div><strong>Operação em dia</strong><p>Nenhuma pendência identificada para esta fazenda.</p></div></div>}
      } @else if(store.attention().status==='error'){<gr-error-state title="Não foi possível carregar as pendências" [description]="errorText(store.attention().error?.message)" [reference]="reference(store.attention().error?.requestId)" (retry)="store.retry('attention')" />}@else{<div class="queue-skeleton"><gr-skeleton /><gr-skeleton /><gr-skeleton /></div>}
      <a class="section-link" routerLink="/rebanho/agenda">Abrir agenda <span aria-hidden="true">→</span></a>
    </section>
    <section class="surface-panel agenda-section" aria-labelledby="agenda-title">
      <div class="section-heading"><div><span class="section-kicker">PRÓXIMOS PASSOS</span><h2 id="agenda-title">Agenda operacional</h2></div><a class="inline-link" routerLink="/rebanho/agenda">Ver agenda <span aria-hidden="true">→</span></a></div>
      @if(store.agenda().status==='ready'&&store.agenda().value;as agenda){
        @if(agenda.items.length){<div class="agenda-list">@for(item of agenda.items;track item.stableId){@let entry=queue(item);<div class="agenda-row"><time [attr.datetime]="entry.date"><strong>{{day(entry.date)}}</strong><small>{{month(entry.date)}}</small></time><span class="agenda-axis" [attr.data-domain]="domain(entry.kind)" aria-hidden="true"></span><div class="agenda-copy"><strong>{{entry.title}}</strong><span>{{entry.context || kindLabel(entry.kind)}} · {{sourceLabel(entry.source)}}</span></div></div>}</div>}
        @else{<div class="compact-empty"><strong>Nada programado a partir de hoje</strong><p>Os próximos itens da operação aparecerão aqui.</p></div>}
      } @else if(store.agenda().status==='error'){<gr-error-state title="Não foi possível carregar a agenda" [description]="errorText(store.agenda().error?.message)" [reference]="reference(store.agenda().error?.requestId)" (retry)="store.retry('agenda')" />}@else{<div class="queue-skeleton"><gr-skeleton /><gr-skeleton /></div>}
    </section>
  </div>`,
  styles: [`
    :host{display:block;min-width:0;height:100%}.action-column{min-width:0;height:100%;display:grid;grid-template-rows:minmax(13rem,1.1fr) minmax(11rem,.9fr);gap:1rem}.surface-panel{min-width:0;padding:1rem;border:1px solid var(--color-border);border-radius:var(--radius-lg);background:var(--color-surface);box-shadow:var(--shadow-raised)}.section-heading{display:flex;align-items:start;justify-content:space-between;gap:.75rem;margin-bottom:.7rem}.section-heading h2{margin:.15rem 0 0;font-size:1rem;letter-spacing:-.025em}.section-kicker{display:block;color:var(--color-primary);font-size:.625rem;font-weight:780;letter-spacing:.1em}.attention-kicker{color:var(--color-warning)}.attention-section{display:flex;min-height:0;flex-direction:column}.attention-count{width:1.65rem;height:1.65rem;display:grid;place-items:center;border-radius:50%;color:var(--color-warning);background:var(--color-warning-subtle);font-size:.7rem;font-weight:800}.attention-count.empty{color:var(--color-success);background:var(--color-success-subtle)}.queue{max-height:12rem;display:grid;overflow:auto;overscroll-behavior:contain}.queue-entry{border-bottom:1px solid var(--color-border)}.queue-button{width:100%;display:grid;grid-template-columns:.45rem minmax(0,1fr) auto .65rem;align-items:start;gap:.55rem;padding:.68rem .2rem;border:0;border-radius:var(--radius-sm);color:var(--color-text);background:transparent;text-align:left;cursor:pointer;transition:background var(--duration-fast)}.queue-button:hover,.queue-button[aria-expanded=true]{background:color-mix(in srgb,var(--color-warning-subtle) 42%,transparent)}.domain-dot{width:.45rem;height:.45rem;margin-top:.32rem;border-radius:50%;background:var(--color-primary)}.domain-dot[data-domain=health]{background:var(--data-amber)}.domain-dot[data-domain=weight]{background:var(--data-violet)}.domain-dot[data-domain=reproduction]{background:#80658e}.domain-dot[data-domain=movement]{background:var(--color-info)}.critical .domain-dot{background:var(--color-danger)}.queue-copy{min-width:0}.queue-copy strong,.agenda-copy strong{display:block;font-size:.74rem;line-height:1.3}.queue-copy small,.agenda-copy span{display:block;margin-top:.15rem;overflow:hidden;color:var(--color-text-muted);font-size:.62rem;text-overflow:ellipsis;white-space:nowrap}.queue-button time{color:var(--color-text-secondary);font-size:.625rem;font-variant-numeric:tabular-nums}.queue-chevron{color:var(--color-text-muted)}.queue-detail{display:grid;gap:.15rem;padding:0 .75rem .6rem 1rem;color:var(--color-text-secondary);font-size:.65rem}.attention-clear{min-height:5rem;display:flex;align-items:center;gap:.75rem;padding:.75rem;color:var(--color-success)}.attention-clear>span{width:1.8rem;height:1.8rem;display:grid;place-items:center;border-radius:50%;background:var(--color-success-subtle);font-weight:800}.attention-clear strong{font-size:.75rem}.attention-clear p{margin:.1rem 0 0;font-size:.65rem}.section-link,.inline-link{display:inline-flex;align-items:center;gap:.35rem;color:var(--color-primary);font-size:.65rem;font-weight:720;text-decoration:none}.section-link{align-self:flex-start;margin-top:auto;padding-top:.65rem}.section-link:hover,.inline-link:hover{text-decoration:underline}.agenda-list{max-height:10rem;overflow:auto}.agenda-row{display:grid;grid-template-columns:2.25rem .5rem minmax(0,1fr);align-items:center;gap:.55rem;min-height:3.15rem}.agenda-row time{display:grid;color:var(--color-primary);font-variant-numeric:tabular-nums}.agenda-row time strong{font-size:.9rem;line-height:1}.agenda-row time small{color:var(--color-text-muted);font-size:.55rem;text-transform:uppercase}.agenda-axis{position:relative;width:1px;height:100%;justify-self:center;background:var(--color-border)}.agenda-axis:before{position:absolute;top:50%;left:50%;width:.45rem;height:.45rem;border:2px solid var(--color-surface);border-radius:50%;background:var(--color-primary);content:'';transform:translate(-50%,-50%)}.agenda-axis[data-domain=health]:before{background:var(--data-amber)}.agenda-axis[data-domain=weight]:before{background:var(--data-violet)}.agenda-axis[data-domain=reproduction]:before{background:#80658e}.agenda-axis[data-domain=movement]:before{background:var(--color-info)}.agenda-copy{min-width:0;padding:.55rem 0;border-bottom:1px solid var(--color-border)}.compact-empty{min-height:5rem;display:grid;align-content:center;padding:.75rem;border-block:1px solid var(--color-border)}.compact-empty strong{font-size:.75rem}.compact-empty p{margin:.15rem 0 0;font-size:.65rem}.queue-skeleton{display:grid;gap:.6rem}.queue-skeleton gr-skeleton{height:2rem}@media(max-width:64rem){.action-column{grid-template-columns:1fr 1fr;grid-template-rows:auto}}@media(max-width:52rem){.action-column{grid-template-columns:1fr}}@media(max-width:40rem){.surface-panel{padding:.8rem}}
  `],
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class HomeActionsComponent {
  readonly openItem=signal<string|null>(null); queue=mapQueueItem;
  constructor(readonly store:DashboardStore){}
  toggleItem(id:string):void{this.openItem.set(this.openItem()===id?null:id);} formatDate(value?:string):string{if(!value)return'—';const[year,month,day]=value.split('-');return`${day}/${month}/${year}`;} shortDate(value:string):string{const[,month,day]=value.split('-');return`${day}/${month}`;} day(value:string):string{return value.slice(8,10);} month(value:string):string{return new Intl.DateTimeFormat('pt-BR',{month:'short',timeZone:'UTC'}).format(new Date(`${value}T12:00:00Z`)).replace('.','');}
  sourceLabel(source:QueueItem['source']):string{return source==='MANUAL'?'Planejado':'Identificado pelos dados';} kindLabel(kind:string):string{return({GENERAL:'Atividade geral',VACCINATION:'Vacinação',DEWORMING:'Vermifugação',WEIGHING:'Pesagem',CALVING:'Parto',BREEDING:'Cobertura',PREGNANCY_CHECK:'Exame de gestação',MOVEMENT:'Movimentação'} as Record<string,string>)[kind]??'Atividade';}
  domain(kind:string):string{return({VACCINATION:'health',DEWORMING:'health',WEIGHING:'weight',CALVING:'reproduction',BREEDING:'reproduction',PREGNANCY_CHECK:'reproduction',MOVEMENT:'movement'} as Record<string,string>)[kind]??'territory';} statusLabel(status:string):string{return({OPEN:'Aberta',OVERDUE:'Atrasada',COMPLETED:'Concluída',CANCELLED:'Cancelada'} as Record<string,string>)[status]??status.toLocaleLowerCase('pt-BR');} errorText(value?:string):string{return value||'Verifique sua conexão e tente novamente.';} reference(value?:string):string{return value?.slice(0,12)||'';}
}
