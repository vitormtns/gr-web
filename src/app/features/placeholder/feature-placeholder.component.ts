import { ChangeDetectionStrategy, Component } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
import { EmptyStateComponent } from '../../design-system/feedback/feedback';
@Component({selector:'app-feature-placeholder',imports:[EmptyStateComponent],template:`<div class="page-enter"><header><span>Área preparada</span><h1>{{title}}</h1></header><section><gr-empty-state [title]="title+' chega em breve'" [description]="'A fundação desta área está pronta. A implementação funcional faz parte da '+phase+'.'" /></section></div>`,styles:[`header{margin-bottom:var(--space-6)}header span{font-size:.6875rem;font-weight:680;letter-spacing:.075em;text-transform:uppercase;color:var(--color-text-muted)}header h1{margin-top:var(--space-1)}section{border:1px solid var(--color-border);border-radius:var(--radius-lg);background:var(--color-surface)}`],changeDetection:ChangeDetectionStrategy.OnPush})
export class FeaturePlaceholderComponent{readonly title:string;readonly phase:string;constructor(route:ActivatedRoute){this.title=route.snapshot.data['title'] as string;this.phase=route.snapshot.data['phase'] as string;}}
